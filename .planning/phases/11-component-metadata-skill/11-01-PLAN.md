---
phase: 11-component-metadata-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [SKILL.md]
autonomous: true
requirements: [META-01, META-02, META-03, META-04]
user_setup: []
must_haves:
  truths:
    - SKILL.md 包含所有必需的 YAML frontmatter 字段
    - SKILL.md type 字段设置为 "communication"
    - SKILL.md 声明对 comm-bridge 组件的依赖
    - SKILL.md config.required 定义了 GITHUB_WEBHOOK_SECRET 参数
    - SKILL.md description 包含清晰的触发模式描述
    - YAML frontmatter 语法正确，可被解析
  artifacts:
    - path: "SKILL.md"
      provides: "组件元数据和配置架构声明"
      contains: "dependencies: [comm-bridge]"
      contains: "type: communication"
      contains: "config.required[0].name: GITHUB_WEBHOOK_SECRET"
  key_links:
    - from: "SKILL.md frontmatter"
      to: "Zylos CLI discovery"
      via: "SKILL.md 文件被 Zylos CLI 解析"
      pattern: "name|version|type|dependencies"
    - from: "SKILL.md config.required"
      to: "Zylos configuration collector"
      via: "Zylos 收集必需的配置值"
      pattern: "config.required.*name.*description"
---

## Phase Goal

**As a** Zylos 系统管理员，**I want to** SKILL.md 包含完整的组件元数据和配置架构，**so that** Zylos CLI 可以正确发现和管理 github-connector 组件。

## Objective

完善 SKILL.md 组件元数据文件，使其符合 Zylos 组件标准，确保 Zylos CLI 能够正确识别组件类型、依赖关系和配置要求。

Purpose: 组件元数据是 Zylos 生态系统自动发现和组件管理的核心依据。缺少 `dependencies` 声明会导致 Zylos 无法正确初始化 comm-bridge 依赖；不清晰的 description 会导致 Claude 无法准确选择此组件处理用户请求。

Output: 更新的 SKILL.md 文件，包含完整的 frontmatter 元数据和准确的配置架构声明。

## Execution Context

参考模式文件（已在 11-PATTERNS.md 中提取）：
- `docs/reference/zylos-telegram/SKILL.md` — 完整的通信组件实现示例
- `docs/reference/zylos-component-template/template/SKILL.md` — 标准 SKILL.md 模板

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-component-metadata-skill/CONTEXT.md
@.planning/phases/11-component-metadata-skill/11-PATTERNS.md

## Tasks

<task type="auto">
  <name>Task 1: 添加 dependencies 字段到 frontmatter</name>
  <files>SKILL.md</files>
  <action>
在 SKILL.md 的 YAML frontmatter 中添加 dependencies 字段。根据 zylos-telegram 参考实现（line 41-43），dependencies 字段应位于 config 部分之后、--- 分隔符之前。

具体更改：
1. 在 line 52（config 部分结束后）添加新字段：
dependencies:
  - comm-bridge

2. 确保缩进为 2 空格（与 frontmatter 其他字段保持一致）

3. 确认 dependencies 在 --- 分隔符之前（当前在 line 53）

Do NOT:
- 不要修改 config 部分的内容
- 不要更改 --- 分隔符的位置
- 不要添加除 comm-bridge 以外的其他依赖（根据 CONTEXT.md 决策）
  </action>
  <verify>
# 验证 dependencies 字段存在
grep -A 2 "^dependencies:" SKILL.md

# 验证 dependencies 在 --- 之前
awk '/^dependencies:/{found=1} /^---/{if(found) exit 1} END{exit !found}' SKILL.md

# 验证 comm-bridge 依赖声明
grep "comm-bridge" SKILL.md
  </verify>
  <done>
- dependencies: [comm-bridge] 字段存在于 SKILL.md frontmatter
- dependencies 字段位于 config 部分、--- 分隔符之前
- YAML 语法正确，可被解析
  </done>
</task>

<task type="auto">
  <name>Task 2: 改进 description 字段以包含触发模式</name>
  <files>SKILL.md</files>
  <action>
更新 SKILL.md 的 description 字段（lines 4-6），参考 zylos-telegram 的编号列表风格（lines 4-12），使用更明确的触发模式描述。

当前 description：
description: >
  GitHub Webhook 连接器，用于 Zylos AI Agent 平台。当用户需要接收 GitHub 仓库事件通知、
  通过通信通道发送 GitHub 活动消息、配置 Webhook 设置时使用此组件。

更改为：
description: >-
  GitHub Webhook connector for Zylos AI Agent platform (单向：GitHub → Zylos，通过 webhook 接收事件).
  Use when: (1) receiving GitHub webhook events (issues, pull requests, comments, releases),
  (2) sending notifications to configured communication endpoints,
  (3) configuring webhook secret and server port,
  (4) troubleshooting webhook signature verification or event delivery.
  Config at ~/zylos/components/github-connector/config.json. Service: pm2 zylos-github-connector.

关键更改：
1. 使用 >- 代替 >（折叠标量，换行替换为空格）
2. 使用英文描述（与 zylos-telegram 保持一致）
3. 添加编号的 "Use when" 列表：(1) ... (2) ... (3) ... (4) ...
4. 在末尾添加配置位置和服务管理提示：Config at ... Service: ...

Do NOT:
- 不要将 description 翻译为中文（Zylos 生态使用英文元数据）
- 不要使用超过 4 个编号项（保持简洁）
- 不要移除现有的核心信息（GitHub Webhook、单向流）
  </action>
  <verify>
# 验证 description 包含 "Use when:" 关键词
grep "Use when:" SKILL.md

# 验证 description 包含配置位置提示
grep "Config at" SKILL.md

# 验证 description 包含服务管理提示
grep "Service: pm2" SKILL.md

# 验证 YAML 语法
ruby -ryaml -e "YAML.load_file('SKILL.md')" 2>&1
  </verify>
  <done>
- description 字段使用 >- 折叠标量格式
- description 包含 4 个编号的触发模式：(1) receiving ... (2) sending ... (3) configuring ... (4) troubleshooting ...
- description 以配置位置和服务管理提示结尾
- YAML 语法正确，ruby YAML 解析器无错误
  </done>
</task>

<task type="auto">
  <name>Task 3: 在文档正文添加依赖说明</name>
  <files>SKILL.md</files>
  <action>
在 SKILL.md 文档正文的开头（line 57 之后，# GitHub Webhook 连接器 标题之后）添加依赖说明，参考 zylos-telegram（line 50）。

在 line 59（## 功能 标题之前）添加：
Depends on: comm-bridge (C4 message routing).

说明：
- 这行文字告诉读者此组件依赖 comm-bridge 进行消息路由
- 与 frontmatter 中的 dependencies 字段保持一致
- 符合 zylos-telegram 的文档模式

Do NOT:
- 不要在 frontmatter 中重复此信息（仅用于文档正文）
- 不要添加其他依赖说明（仅 comm-bridge）
- 不要使用中英文混合（使用英文）
  </action>
  <verify>
# 验证文档正文包含依赖说明
grep "Depends on: comm-bridge" SKILL.md

# 验证依赖说明在 frontmatter 之后
awk '/^---/{in_body=1} in_body && /Depends on:/{found=1} END{exit !found}' SKILL.md
  </verify>
  <done>
- 文档正文包含 "Depends on: comm-bridge (C4 message routing)." 说明
- 依赖说明位于 # GitHub Webhook 连接器 标题之后、## 功能 标题之前
- 依赖说明使用英文，格式与 zylos-telegram 一致
  </done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → Zylos CLI | 信任边界：SKILL.md 被 Zylos CLI 自动解析 |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | SKILL.md frontmatter | accept | SKILL.md 是静态元数据文件，不包含敏感信息。YAML 解析错误会在 Zylos CLI 执行时被捕获，不会导致安全漏洞。 |
| T-11-02 | I | config.required.sensitive | mitigate | 确保 GITHUB_WEBHOOK_SECRET 字段标记为 sensitive: true，防止 Zylos CLI 在日志或输出中泄露密钥值。已在现有 SKILL.md 中正确配置（line 44），无需更改。 |

## Verification

### Overall Phase Checks

1. **元数据完整性验证：**
   ```bash
   # 验证所有必需的 frontmatter 字段存在
   grep -E "^(name|version|description|type|lifecycle|upgrade|config|dependencies):" SKILL.md | wc -l  # 应输出 >= 8
   ```

2. **依赖声明验证：**
   ```bash
   # 验证 dependencies 字段存在且包含 comm-bridge
   grep -A 2 "^dependencies:" SKILL.md | grep "comm-bridge"
   ```

3. **组件类型验证：**
   ```bash
   # 验证 type 设置为 communication
   grep "^type: communication" SKILL.md
   ```

4. **配置架构验证：**
   ```bash
   # 验证 config.required 定义了 webhook secret
   grep -A 3 "^config:" SKILL.md | grep -A 2 "required:" | grep "GITHUB_WEBHOOK_SECRET"
   ```

5. **YAML 语法验证：**
   ```bash
   # 使用 ruby YAML 解析器验证（如果 ruby 不可用，跳过）
   if command -v ruby &> /dev/null; then
     ruby -ryaml -e "puts YAML.load_file('SKILL.md').inspect" 2>&1
   fi
   ```

6. **描述触发模式验证：**
   ```bash
   # 验证 description 包含 "Use when:" 和编号列表
   grep "Use when:" SKILL.md | grep -E "\([1-4]\)"
   ```

## Success Criteria

1. **SKILL.md 包含所有必需字段：** name、version、type、description、lifecycle、upgrade、config、dependencies
2. **type 设置为 "communication"：** 确认 Zylos CLI 将此组件识别为通信组件
3. **声明对 comm-bridge 的依赖：** dependencies 字段包含 comm-bridge，Zylos 会在初始化时确保此依赖可用
4. **config 部分定义 webhook secret 参数：** config.required 定义 GITHUB_WEBHOOK_SECRET，标记为 sensitive
5. **description 包含触发模式：** description 使用编号列表 (1) ... (2) ... (3) ... (4) ... 格式，明确何时使用此组件
6. **YAML 语法正确：** SKILL.md frontmatter 可被 YAML 解析器正确解析

## Output

完成所有任务后，创建 `.planning/phases/11-component-metadata-skill/11-01-SUMMARY.md`：

```markdown
# Plan 11-01 Summary: Component Metadata (SKILL.md)

**Completed:** [date]

## Changes Made

### Added dependencies field
- Location: SKILL.md line 52 (frontmatter)
- Content: dependencies: [comm-bridge]
- Purpose: Declare comm-bridge dependency for Zylos CLI

### Improved description field
- Changed from: Basic Chinese description
- Changed to: English description with numbered trigger patterns
- Format: >- folded scalar with 4 use cases
- Added: Config location and service management hints

### Added dependency documentation in body
- Location: SKILL.md line 59 (after title, before ## 功能)
- Content: "Depends on: comm-bridge (C4 message routing)."

## Requirements Covered

- ✅ META-01: SKILL.md contains component metadata (name, version, type, description, config)
- ✅ META-02: SKILL.md type set to "communication"
- ✅ META-03: SKILL.md declares dependency on comm-bridge
- ✅ META-04: SKILL.md config section defines webhook secret parameter

## Verification Results

- All required frontmatter fields present
- type field correctly set to "communication"
- dependencies field contains comm-bridge
- config.required defines GITHUB_WEBHOOK_SECRET with sensitive: true
- description includes "Use when:" with 4 numbered trigger patterns
- YAML syntax valid

## Next Steps

Phase 11 complete. Proceed to Phase 12 - Documentation and Testing.
```
