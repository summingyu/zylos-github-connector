---
phase: 10-lifecycle-pm2-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ecosystem.config.cjs
  - src/index.js
  - tests/integration/lifecycle.test.js
  - scripts/pm2-test.sh
autonomous: true
requirements:
  - LIFE-01
  - LIFE-02
  - LIFE-03
  - LIFE-04
user_setup: []
must_haves:
  truths:
    - PM2 可以启动和停止服务
    - 优雅关闭正确清理资源（HTTP 服务器、文件监控、定时器）
    - ecosystem.config.cjs 正确配置 PM2 服务
    - 组件在配置中禁用时退出
    - PM2 日志正确记录到指定文件
  artifacts:
    - path: ecosystem.config.cjs
      provides: PM2 进程管理配置
      min_lines: 25
    - path: tests/integration/lifecycle.test.js
      provides: PM2 集成测试
      min_lines: 150
    - path: scripts/pm2-test.sh
      provides: PM2 测试脚本
      min_lines: 50
  key_links:
    - from: ecosystem.config.cjs
      to: src/index.js
      via: "script: 'src/index.js'"
      pattern: "script:\\s*['\"]src/index\\.js['\"]"
    - from: src/index.js
      to: process
      via: "process.on('SIGTERM', ...)"
      pattern: "process\\.on\\(['\"]SIGTERM"
---

<objective>
修复 PM2 集成配置，确保进程管理和生命周期控制正常工作。

Purpose: 使组件可以通过 PM2 启动、停止和重启，实现生产环境的进程管理和监控。
Output: 修复后的 ecosystem.config.cjs、PM2 集成测试和验证脚本。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-lifecycle-pm2-integration/10-RESEARCH.md
@src/index.js
@ecosystem.config.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 ecosystem.config.cjs 路径配置</name>
  <files>ecosystem.config.cjs</files>
  <action>
    修复 ecosystem.config.cjs 中的路径配置问题：

    1. **修复 cwd 路径**：
       - 当前路径：`os.homedir(), 'zylos/.claude/skills/github-connector'`（不存在）
       - 修正为：使用 `__dirname` 或 `process.cwd()` 动态获取项目根目录
       - 确保 PM2 从正确的目录启动应用

    2. **使用动态路径**：
       ```javascript
       const path = require('path');

       module.exports = {
         apps: [{
           name: 'zylos-github-connector',
           script: 'src/index.js',
           cwd: path.join(__dirname), // 项目根目录
           // ...
         }]
       };
       ```

    3. **验证日志路径**：
       - 确保 error_file 和 out_file 使用正确的相对路径
       - 路径应为 `logs/error.log` 和 `logs/out.log`（相对于 cwd）
       - 日志目录将在启动时创建（如果不存在）

    4. **保持其他配置不变**：
       - autorestart: true
       - max_restarts: 10
       - restart_delay: 5000
       - env: { NODE_ENV: 'production' }

    **实现参考**（来自 10-RESEARCH.md Pattern 1: PM2 Ecosystem Configuration）：
    - 使用 path.join(__dirname) 而非硬编码路径
    - 相对路径用于日志文件
    - 保留现有的重启和内存配置

    **不要**：
    - 不要修改应用名称（zylos-github-connector）
    - 不要修改脚本路径（src/index.js）
    - 不要移除日志配置
  </action>
  <verify>
    <automated>cat ecosystem.config.cjs | grep -q "path.join(__dirname)"</automated>
  </verify>
  <done>
    - cwd 使用 path.join(__dirname)
    - 日志路径使用相对路径
    - 配置文件格式正确（有效的 JavaScript）
    - 所有必需字段存在（name, script, cwd）
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证优雅关闭实现</name>
  <files>src/index.js</files>
  <action>
    验证并确保 src/index.js 中的优雅关闭实现正确：

    1. **检查信号处理程序**：
       - 确认 process.on('SIGINT', shutdown) 存在
       - 确认 process.on('SIGTERM', shutdown) 存在
       - 确认 shutdown 函数正确实现

    2. **验证关闭流程**：
       - isShuttingDown 标志防止重复执行
       - 停止文件监控（stopWatching）
       - 关闭 HTTP 服务器（app.close）
       - 清除定时器（dedupeCleanupInterval）
       - 超时保护（10 秒后强制退出）

    3. **添加关闭日志**（如果缺失）：
       ```javascript
       app.log.info('[github-connector] Shutting down...');
       app.log.info('[github-connector] Stopped watching configuration file');
       app.log.info('[github-connector] Server closed gracefully');
       ```

    4. **验证已存在实现**：
       - 检查 src/index.js 第 393-423 行（现有 shutdown 函数）
       - 确认所有资源清理步骤都存在
       - 确认超时保护存在

    **实现参考**（来自 10-RESEARCH.md Pattern 2: Graceful Shutdown with PM2）：
    - 使用 isShuttingDown 标志防止重复执行
    - 超时定时器在 10 秒后强制退出
    - 清理所有资源（监控、服务器、定时器）

    **不要**：
    - 不要修改现有的关闭逻辑（除非有 bug）
    - 不要移除超时保护
    - 不要移除信号处理程序
  </action>
  <verify>
    <automated>grep -q "process.on('SIGINT'" src/index.js && grep -q "process.on('SIGTERM'" src/index.js</automated>
  </verify>
  <done>
    - SIGINT 和 SIGTERM 信号处理程序存在
    - shutdown 函数清理所有资源
    - 超时保护存在（10 秒）
    - 关闭日志完整
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 PM2 集成测试</name>
  <files>tests/integration/lifecycle.test.js</files>
  <action>
    创建 PM2 集成测试，验证进程管理和生命周期：

    1. **测试文件结构**：
       - 使用 Node.js 内置 test runner（node:test）
       - 导入 describe, it, before, after, assert
       - 使用 child_process.execSync 执行 PM2 命令

    2. **测试辅助函数**：
       - startPM2(): 启动 PM2 进程
       - stopPM2(): 停止 PM2 进程
       - deletePM2(): 删除 PM2 进程
       - getPM2Status(): 获取进程状态
       - waitForStatus(status): 等待进程达到指定状态

    3. **测试用例**（至少 10 个测试）：

       **启动测试**：
       - PM2 应成功启动进程
       - 进程状态应为 'online'
       - 日志文件应创建
       - 进程应监听配置端口（3461）

       **停止测试**：
       - pm2 stop 应优雅停止进程
       - 进程状态应为 'stopped'
       - 优雅关闭日志应出现
       - 资源应正确清理

       **重启测试**：
       - pm2 restart 应重启进程
       - 进程状态应为 'online'
       - 重启后服务应正常工作

       **删除测试**：
       - pm2 delete 应移除进程
       - 进程应不在 PM2 列表中
       - 日志文件应保留

       **配置测试**：
       - disabled 标志应触发进程退出
       - 日志级别应正确配置
       - 端口配置应正确应用

    4. **测试清理**：
       - after hook 清理 PM2 进程
       - 确保测试不影响系统 PM2 状态

    **实现参考**（来自 10-RESEARCH.md Code Examples: PM2 Integration Test）：
    - 使用 execSync 执行 PM2 命令
    - 解析 pm2 list 输出验证状态
    - 测试启动、停止、重启、删除操作

    **不要**：
    - 不要测试配置热重载（已在 09-02 测试）
    - 不要测试签名验证（已在 02-06 测试）
  </action>
  <verify>
    <automated>node --test tests/integration/lifecycle.test.js</automated>
  </verify>
  <done>
    - 至少 10 个集成测试
    - 所有测试通过
    - 测试覆盖 PM2 启动、停止、重启、删除
    - 测试验证优雅关闭
    - 测试辅助函数正常工作
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建 PM2 测试脚本</name>
  <files>scripts/pm2-test.sh</files>
  <action>
    创建 Bash 脚本用于手动测试 PM2 集成：

    1. **脚本结构**：
       ```bash
       #!/bin/bash
       set -e  # 遇到错误立即退出

       # 颜色输出
       GREEN='\033[0;32m'
       RED='\033[0;31m'
       NC='\033[0m' # No Color

       # 函数：打印成功/失败消息
       success() { echo -e "${GREEN}✓ $1${NC}"; }
       error() { echo -e "${RED}✗ $1${NC}"; }
       ```

    2. **测试函数**：

       **test_start()**:
       - 停止旧进程（如果存在）
       - 启动 PM2 进程
       - 验证进程状态为 'online'
       - 验证日志文件存在
       - 测试 HTTP 端点（/health）

       **test_stop()**:
       - 停止 PM2 进程
       - 验证进程状态为 'stopped'
       - 检查优雅关闭日志

       **test_restart()**:
       - 重启 PM2 进程
       - 验证进程状态为 'online'
       - 验证服务正常工作

       **test_disabled()**:
       - 修改配置设置 enabled: false
       - 重启进程
       - 验证进程退出
       - 恢复配置

    3. **主测试流程**：
       ```bash
       main() {
         echo "=== PM2 Integration Test Suite ==="
         test_start
         test_stop
         test_restart
         test_disabled
         cleanup
         echo "=== All tests passed ==="
       }
       ```

    4. **清理函数**：
       - 删除 PM2 进程
       - 恢复配置文件
       - 清理临时文件

    **实现参考**（来自 10-RESEARCH.md Code Examples: PM2 Startup Script）：
    - 使用 set -e 确保错误立即退出
    - 使用颜色输出提高可读性
    - 测试启动、停止、重启、禁用场景

    **不要**：
    - 不要在 CI/CD 环境中使用（仅用于本地测试）
    - 不要修改系统 PM2 配置
  </action>
  <verify>
    <automated>bash scripts/pm2-test.sh</automated>
  </verify>
  <done>
    - 脚本可执行（chmod +x）
    - 测试启动、停止、重启、禁用
    - 清理函数正确工作
    - 颜色输出提高可读性
  </done>
</task>

<task type="auto">
  <name>Task 5: 验证 PM2 集成端到端</name>
  <files>ecosystem.config.cjs, src/index.js</files>
  <action>
    执行端到端验证，确保 PM2 集成正常工作：

    1. **启动验证**：
       ```bash
       pm2 delete zylos-github-connector 2>/dev/null || true
       pm2 start ecosystem.config.cjs
       pm2 list | grep zylos-github-connector
       pm2 logs zylos-github-connector --lines 10
       ```

    2. **功能验证**：
       - 访问 http://localhost:3461/health
       - 发送测试 webhook
       - 验证日志记录

    3. **停止验证**：
       ```bash
       pm2 stop zylos-github-connector
       pm2 list | grep zylos-github-connector
       pm2 logs zylos-github-connector --lines 10
       ```

    4. **重启验证**：
       ```bash
       pm2 restart zylos-github-connector
       pm2 list | grep zylos-github-connector
       curl http://localhost:3461/health
       ```

    5. **清理**：
       ```bash
       pm2 delete zylos-github-connector
       pm2 list
       ```

    **验证标准**：
    - PM2 可以启动进程
    - 进程状态正确显示
    - 日志正确记录
    - 停止/重启/删除命令正常工作

    **不要**：
    - 不要在生产环境执行（仅开发/测试环境）
  </action>
  <verify>
    <automated>pm2 list | grep -q "zylos-github-connector" || pm2 start ecosystem.config.cjs && pm2 delete zylos-github-connector</automated>
  </verify>
  <done>
    - PM2 启动成功
    - 进程状态为 'online'
    - 日志正确记录
    - 停止/重启/删除正常工作
    - /health 端点可访问
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PM2 进程 → 应用 | PM2 管理应用生命周期，必须正确处理信号 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | 日志文件增长 | mitigate | 使用 pm2-logrotate 或定期清理日志 |
| T-10-02 | Information Disclosure | 日志文件权限 | mitigate | 设置日志目录权限为 0700，文件为 0600 |
| T-10-03 | Privilege Escalation | PM2 进程权限 | mitigate | 以非 root 用户运行 PM2 |
</threat_model>

<verification>
1. 所有 PM2 集成测试通过（至少 10 个测试）
2. PM2 启动/停止/重启命令正常工作
3. 优雅关闭日志完整
4. 进程状态正确显示
5. /health 端点可访问
6. 日志文件正确创建和记录
</verification>

<success_criteria>
1. PM2 可以启动和停止服务（LIFE-01）
2. 优雅关闭正确清理资源（LIFE-02）
3. ecosystem.config.cjs 定义 PM2 服务（LIFE-03）
4. 组件在配置中禁用时退出（LIFE-04）
5. PM2 日志正确记录到文件
6. 所有集成测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-lifecycle-pm2-integration/10-01-SUMMARY.md`
</output>
