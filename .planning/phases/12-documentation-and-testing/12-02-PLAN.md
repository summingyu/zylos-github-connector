---
phase: 12-documentation-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
user_setup: []

must_haves:
  truths:
    - "组件包含签名验证测试（有效和无效签名）"
    - "组件包含事件类型解析测试"
    - "组件包含传递 ID 去重测试"
    - "所有测试通过（519+ 个测试）"
    - "README.md 的测试部分反映实际的测试覆盖"
  artifacts:
    - path: "src/lib/__tests__/verifier.test.js"
      provides: "签名验证测试（有效、无效、格式错误）"
      contains: "describe('签名验证模块')"
    - path: "src/lib/__tests__/event-parser.test.js"
      provides: "事件类型解析测试"
      contains: "describe('Event Parser Module')"
    - path: "src/lib/__tests__/dedupe.test.js"
      provides: "传递 ID 去重测试"
      contains: "describe('Deduplication Module')"
  key_links:
    - from: "README.md 测试部分"
      to: "src/lib/__tests__/*.test.js"
      via: "测试文档引用"
      pattern: "npm test|tests/"
    - from: "package.json"
      to: "src/lib/__tests__/*.test.js"
      via: "test 脚本"
      pattern: "\"test\":"

---

<objective>
验证组件的测试覆盖满足 TEST-01、TEST-02、TEST-03 要求，并更新 README.md 的测试部分以反映实际的测试覆盖情况。

目的：确保项目的测试覆盖满足需求文档的要求，为生产就绪提供信心。

输出：验证报告（通过/失败的测试列表）、更新的 README.md 测试部分。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@package.json
@README.md

# 测试要求（来自 REQUIREMENTS.md）

- **TEST-01**：组件包含签名验证测试（有效和无效签名）
- **TEST-02**：组件包含事件类型解析测试
- **TEST-03**：组件包含传递 ID 去重测试

# 当前测试状态（来自 STATE.md 和实际运行）

根据 `npm test` 运行结果：
- 总计：519 个测试
- 通过：519 个测试
- 失败：0 个测试

已识别的测试文件：
- src/lib/__tests__/verifier.test.js（签名验证）
- src/lib/__tests__/event-parser.test.js（事件解析）
- src/lib/__tests__/dedupe.test.js（去重）
- src/lib/__tests__/*.test.js（其他测试：handlers、integration、comm-bridge 等）

# 当前 README.md 的测试部分

README.md 中的"测试"部分（约第 193-218 行）需要更新以反映实际的 519+ 测试覆盖。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证签名验证测试存在（TEST-01）</name>
  <files>src/lib/__tests__/verifier.test.js</files>
  <action>
    验证 src/lib/__tests__/verifier.test.js 文件存在并包含签名验证测试。

    运行以下检查：
    1. 检查文件是否存在：ls -la src/lib/__tests__/verifier.test.js
    2. 检查测试覆盖：
       - 有效签名验证测试（valid signature）
       - 无效签名验证测试（invalid signature）
       - 格式错误处理测试（malformed signature）
       - 常量时间比较测试（timingSafeEqual）
       - HMAC 计算测试（computeHmac）

    运行测试验证功能：
    ```bash
    npm test 2>&1 | grep -A 5 "签名验证模块"
    ```

    如果测试文件不存在或测试覆盖不完整，记录缺失的测试用例。
  </action>
  <verify>
    <automated>ls src/lib/__tests__/verifier.test.js && grep -q "describe('签名验证模块" src/lib/__tests__/verifier.test.js</automated>
  </verify>
  <done>签名验证测试文件存在，包含有效和无效签名测试用例</done>
</task>

<task type="auto">
  <name>Task 2: 验证事件类型解析测试存在（TEST-02）</name>
  <files>src/lib/__tests__/event-parser.test.js</files>
  <action>
    验证 src/lib/__tests__/event-parser.test.js 文件存在并包含事件类型解析测试。

    运行以下检查：
    1. 检查文件是否存在：ls -la src/lib/__tests__/event-parser.test.js
    2. 检查测试覆盖：
       - X-GitHub-Event 头解析测试（getEventType）
       - X-GitHub-Delivery 头解析测试（getDeliveryId）
       - 未知事件类型处理测试
       - 缺失头处理测试

    运行测试验证功能：
    ```bash
    npm test 2>&1 | grep -A 5 "Event Parser Module"
    ```

    如果测试文件不存在或测试覆盖不完整，记录缺失的测试用例。
  </action>
  <verify>
    <automated>ls src/lib/__tests__/event-parser.test.js && grep -q "describe('Event Parser Module" src/lib/__tests__/event-parser.test.js</automated>
  </verify>
  <done>事件类型解析测试文件存在，包含事件解析和 delivery ID 提取测试用例</done>
</task>

<task type="auto">
  <name>Task 3: 验证传递 ID 去重测试存在（TEST-03）</name>
  <files>src/lib/__tests__/dedupe.test.js</files>
  <action>
    验证 src/lib/__tests__/dedupe.test.js 文件存在并包含传递 ID 去重测试。

    运行以下检查：
    1. 检查文件是否存在：ls -la src/lib/__tests__/dedupe.test.js
    2. 检查测试覆盖：
       - 未见过的 delivery ID 测试（hasDeliveryBeenSeen 返回 false）
       - 已见过的 delivery ID 测试（hasDeliveryBeenSeen 返回 true）
       - 标记 delivery 为已见测试（markDeliveryAsSeen）
       - null/undefined delivery ID 处理测试
       - seenDeliveries Map 初始化测试

    运行测试验证功能：
    ```bash
    npm test 2>&1 | grep -A 5 "Deduplication Module"
    ```

    如果测试文件不存在或测试覆盖不完整，记录缺失的测试用例。
  </action>
  <verify>
    <automated>ls src/lib/__tests__/dedupe.test.js && grep -q "describe('Deduplication Module" src/lib/__tests__/dedupe.test.js</automated>
  </verify>
  <done>传递 ID 去重测试文件存在，包含去重逻辑和 delivery ID 跟踪测试用例</done>
</task>

<task type="auto">
  <name>Task 4: 运行完整测试套件并验证通过率</name>
  <files>package.json, src/lib/__tests__/**/*.test.js</files>
  <action>
    运行完整的测试套件以验证所有测试通过。

    执行测试：
    ```bash
    npm test
    ```

    验证以下内容：
    1. 测试总数：519+ 个测试
    2. 通过率：100%（无失败测试）
    3. 测试套件数：124+ 个套件
    4. 无跳过（skipped）或待办（todo）测试

    如果有测试失败，记录失败的测试用例和失败原因。
    如果测试总数少于 519，记录可能缺失的测试。

    生成测试摘要：
    - 总测试数
    - 通过数
    - 失败数
    - 测试覆盖的模块列表
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 | grep -E "# tests (519|[5-9][0-9]{2}|[0-9]{3,})" && npm test 2>&1 | tail -10 | grep "# pass 519"</automated>
  </verify>
  <done>所有 519+ 个测试通过，无失败测试</done>
</task>

<task type="auto">
  <name>Task 5: 生成测试覆盖报告</name>
  <files>src/lib/__tests__/**/*.test.js</files>
  <action>
    分析所有测试文件，生成测试覆盖报告。

    运行以下命令收集测试信息：
    ```bash
    # 列出所有测试文件
    find src/lib/__tests__ -name "*.test.js" -type f

    # 统计每个测试文件的测试用例数
    for file in src/lib/__tests__/*.test.js; do
      echo "=== $file ==="
      grep -c "it(" "$file" 2>/dev/null || echo "0"
    done
    ```

    创建测试覆盖矩阵：
    | 模块 | 测试文件 | 测试用例数 | 覆盖的功能 |
    |------|---------|-----------|-----------|
    | 签名验证 | verifier.test.js | XX | 有效/无效签名、HMAC 计算、常量时间比较 |
    | 事件解析 | event-parser.test.js | XX | 事件类型、delivery ID |
    | 去重 | dedupe.test.js | XX | delivery ID 跟踪、重复检测 |
    | Issues 处理 | issues-handler.test.js | XX | opened、closed、reopened |
    | PR 处理 | pull-request-handler.test.js | XX | opened、closed、merged、ready_for_review |
    | Comment 处理 | comment-handler.test.js | XX | created |
    | Release 处理 | release-handler.test.js | XX | published |
    | 通信桥 | comm-bridge.test.js | XX | C4 消息传递、重试 |
    | 配置 | config-loader.test.js, config-watcher.test.js | XX | 加载、验证、热重载 |
    | 生命周期 | lifecycle.test.js | XX | PM2 启动/停止、优雅关闭 |

    将报告保存为临时文件或在控制台输出。
  </action>
  <verify>
    <automated>find src/lib/__tests__ -name "*.test.js" -type f | wc -l | grep -E "^([1-9][0-9]|[0-9]{3,})"</automated>
  </verify>
  <done>测试覆盖报告生成，显示每个模块的测试文件和测试用例数</done>
</task>

<task type="auto">
  <name>Task 6: 更新 README.md 的测试部分</name>
  <files>README.md</files>
  <action>
    更新 README.md 中的"测试"部分（约第 193-218 行），反映实际的测试覆盖情况。

    更新内容：
    1. 测试总数：从"27 个单元测试"更新为"519+ 个测试（单元测试 + 集成测试）"
    2. 测试覆盖列表：
       - ✓ 签名验证（有效、无效、格式错误、常量时间比较）
       - ✓ 事件类型解析（X-GitHub-Event、X-GitHub-Delivery）
       - ✓ 传递 ID 去重（重复检测、Map 跟踪）
       - ✓ Issues 事件处理（opened、closed、reopened）
       - ✓ Pull Request 事件处理（opened、closed、merged、ready_for_review）
       - ✓ Issue Comment 事件处理（created）
       - ✓ Release 事件处理（published）
       - ✓ 消息格式化（URL 格式化、动作标签）
       - ✓ C4 通信桥集成（消息传递、重试逻辑）
       - ✓ 配置管理（加载、验证、热重载）
       - ✓ 生命周期（PM2 启动/停止、优雅关闭）

    3. 添加测试文件结构说明：
       ```bash
       测试文件结构：
       src/lib/__tests__/
       ├── verifier.test.js          # 签名验证测试
       ├── event-parser.test.js      # 事件解析测试
       ├── dedupe.test.js            # 去重测试
       ├── issues-handler.test.js    # Issues 处理测试
       ├── pull-request-handler.test.js  # PR 处理测试
       ├── comment-handler.test.js   # Comment 处理测试
       ├── release-handler.test.js   # Release 处理测试
       ├── comm-bridge.test.js       # 通信桥测试
       └── *.test.js                 # 其他集成测试
       ```

    4. 更新测试文档引用：tests/README.md
  </action>
  <verify>
    <automated>grep -A 20 "## 测试" README.md | grep -E "(519|签名验证|事件解析|去重)" | wc -l | grep -q "^4"</automated>
  </verify>
  <done>"测试"部分反映实际的 519+ 测试覆盖，列出所有测试模块</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 生产代码 | 测试验证生产代码的正确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Spoofing | 测试数据 | accept | 测试使用模拟数据，不影响生产 |
| T-12-05 | Tampering | 测试覆盖报告 | accept | 测试覆盖报告仅用于验证，不影响生产逻辑 |
| T-12-06 | Information Disclosure | 测试日志 | mitigate | 测试日志不包含敏感信息（webhook secret） |
</threat_model>

<verification>
## 整体验证检查

1. **测试文件存在验证** — 验证所有必需的测试文件存在：
   ```bash
   ls src/lib/__tests__/verifier.test.js
   ls src/lib/__tests__/event-parser.test.js
   ls src/lib/__tests__/dedupe.test.js
   ```
   预期输出：所有文件存在

2. **测试内容验证** — 验证测试文件包含正确的测试用例：
   ```bash
   grep -q "签名验证" src/lib/__tests__/verifier.test.js
   grep -q "Event Parser" src/lib/__tests__/event-parser.test.js
   grep -q "Deduplication" src/lib/__tests__/dedupe.test.js
   ```
   预期输出：所有匹配成功

3. **测试通过验证** — 验证所有测试通过：
   ```bash
   npm test 2>&1 | grep "# pass" | grep -E "(519|[5-9][0-9]{2}|[0-9]{3,})"
   ```
   预期输出：519+ 个测试通过

4. **README 更新验证** — 验证 README.md 的测试部分已更新：
   ```bash
   grep -A 20 "## 测试" README.md | grep "519"
   ```
   预期输出：至少 1 个匹配

5. **测试覆盖矩阵验证** — 验证测试覆盖矩阵包含所有核心模块：
   ```bash
   grep -E "(签名验证|事件解析|去重|Issues|Pull Request|Comment|Release|通信桥|配置|生命周期)" README.md
   ```
   预期输出：所有模块都有提及
</verification>

<success_criteria>
## 成功标准

1. ✅ src/lib/__tests__/verifier.test.js 存在并包含有效和无效签名测试
2. ✅ src/lib/__tests__/event-parser.test.js 存在并包含事件类型解析测试
3. ✅ src/lib/__tests__/dedupe.test.js 存在并包含传递 ID 去重测试
4. ✅ 所有 519+ 个测试通过（0 失败）
5. ✅ 测试覆盖报告生成，显示每个模块的测试文件和测试用例数
6. ✅ README.md 的测试部分更新为"519+ 个测试"
7. ✅ README.md 的测试部分列出所有核心模块的测试覆盖
8. ✅ TEST-01、TEST-02、TEST-03 要求全部满足
9. ✅ 测试文档引用正确（tests/README.md）
</success_criteria>

<output>
完成后创建 `.planning/phases/12-documentation-and-testing/12-02-SUMMARY.md`
</output>
