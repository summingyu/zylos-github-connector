---
phase: 12-documentation-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
user_setup: []

must_haves:
  truths:
    - "README.md 包含安装说明，用户能够按照文档完成组件安装"
    - "README.md 包含配置说明，说明端口、secret、端点的配置方法"
    - "README.md 包含 GitHub Webhook 设置说明，用户了解如何在 GitHub 配置 webhook"
    - "README.md 反映项目的当前状态（Phase 11 完成，准备进入 Phase 12）"
  artifacts:
    - path: "README.md"
      provides: "项目文档，包含安装、配置、GitHub Webhook 设置说明"
      contains: "## 安装、## 配置、## GitHub Webhook 设置、## 当前状态"
  key_links:
    - from: "README.md"
      to: "SKILL.md"
      via: "文档交叉引用"
      pattern: "SKILL.md"
    - from: "README.md"
      to: "~/zylos/components/github-connector/config.json"
      via: "配置文件路径说明"
      pattern: "config.json"

---

<objective>
更新 README.md 以反映项目的完整状态和最终文档要求，确保包含安装、配置和 GitHub Webhook 设置的完整说明。

目的：作为项目的最后阶段，提供用户友好的完整文档，使新用户能够顺利安装、配置和使用 GitHub Webhook 连接器。

输出：更新的 README.md，反映 Phase 11 完成状态，包含所有必需的文档部分。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SKILL.md
@README.md

# 项目已完成的核心功能

从 STATE.md 和 ROADMAP.md 可知，项目已完成：
- Phase 1: HTTP 服务器基础（Fastify、原始体捕获、安全头）
- Phase 2: 签名验证（HMAC-SHA256、常量时间比较）
- Phase 3: 事件路由和去重（X-GitHub-Event、X-GitHub-Delivery）
- Phase 4: Issues 事件处理程序
- Phase 5: Pull Request 事件处理程序
- Phase 6: Comment 和 Release 事件处理程序
- Phase 7: 消息格式化模块
- Phase 8: C4 通信桥集成
- Phase 9: 配置管理（热重载、验证）
- Phase 10: 生命周期和 PM2 集成
- Phase 11: 组件元数据（SKILL.md）

# 当前 README.md 的状态

README.md 已存在但需要更新：
- "当前状态"显示为"Phase 2 已完成"（需要更新为 Phase 11）
- 缺少对已完成功能的完整描述（事件处理程序、通信桥、配置热重载等）
- 缺少对支持的 GitHub 事件的详细说明

# 测试覆盖状态

根据 STATE.md 和测试运行结果：
- 519+ 个测试全部通过
- 测试覆盖：签名验证、事件解析、去重、配置管理、生命周期、所有事件处理程序
- 测试文档已存在：tests/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 README.md 的"当前状态"部分</name>
  <files>README.md</files>
  <action>
    更新 README.md 中的"当前状态"部分（约第 47-59 行），将"Phase 2 已完成"改为"Phase 11 已完成，准备进入 Phase 12（最后阶段）"。

    更新"已完成功能"列表，添加以下功能：
    - ✓ 事件路由和去重（X-GitHub-Event、X-GitHub-Delivery）
    - ✓ Issues 事件处理程序（opened、closed、reopened）
    - ✓ Pull Request 事件处理程序（opened、closed、merged、ready_for_review）
    - ✓ Issue Comment 事件处理程序（created）
    - ✓ Release 事件处理程序（published）
    - ✓ 消息格式化模块（一致的格式、可点击 URL）
    - ✓ C4 通信桥集成（异步消息传递）
    - ✓ 配置管理和热重载（深度默认值合并、验证）
    - ✓ PM2 生命周期集成（优雅关闭、进程管理）
    - ✓ 组件元数据（SKILL.md）

    更新"下一步"为"Phase 12 — 文档和测试验证（最后阶段）"。

    更新测试覆盖数量为"519+ 个测试"。
  </action>
  <verify>
    <automated>grep -A 20 "## 当前状态" README.md | grep -q "Phase 11 已完成"</automated>
  </verify>
  <done>"当前状态"部分反映 Phase 11 完成状态，列出所有已完成的核心功能</done>
</task>

<task type="auto">
  <name>Task 2: 完善"功能特性"部分</name>
  <files>README.md</files>
  <action>
    更新 README.md 中的"功能特性"部分（约第 33-41 行），添加以下功能：
    - **事件处理** — 支持 issues、pull_request、issue_comment、release 事件
    - **消息格式化** — 一致的通知格式，包含可点击的 GitHub URL
    - **去重机制** — 基于 X-GitHub-Delivery ID 的内存去重
    - **异步通信** — 通过 C4 通信桥异步传递通知
    - **配置验证** — 深度默认值合并和配置验证

    保留已有的功能特性（GitHub 事件接收、签名验证、健康检查、配置热重载、优雅关闭、安全防护、大负载支持）。
  </action>
  <verify>
    <automated>grep -A 15 "## 功能特性" README.md | grep -q "事件处理"</automated>
  </verify>
  <done>"功能特性"部分包含所有核心功能的简洁描述</done>
</task>

<task type="auto">
  <name>Task 3: 验证并完善安装说明（DOC-01）</name>
  <files>README.md</files>
  <action>
    验证 README.md 中的"安装"部分（约第 63-83 行）是否包含：
    1. 通过 Zylos CLI 安装的方法（推荐）
    2. 手动安装的步骤（git clone、npm install）
    3. 系统要求（Node.js >= 20.0.0、PM2）

    如果缺少任何部分，添加相应内容。确保安装步骤清晰、可执行。

    添加安装后验证的命令：
    ```bash
    # 验证安装
    npm test

    # 检查配置文件
    cat ~/zylos/components/github-connector/config.json
    ```
  </action>
  <verify>
    <automated>grep -A 20 "## 安装" README.md | grep -E "(zylos add|npm install|Node.js|PM2)" | wc -l | grep -q "^[3-9]"</automated>
  </verify>
  <done>"安装"部分包含所有必需的安装方法和系统要求</done>
</task>

<task type="auto">
  <name>Task 4: 验证并完善配置说明（DOC-02）</name>
  <files>README.md</files>
  <action>
    验证 README.md 中的"配置"部分（约第 86-127 行）是否包含：
    1. 配置文件路径：~/zylos/components/github-connector/config.json
    2. 完整的配置示例 JSON
    3. 配置选项表格（包含选项、类型、必需、默认值、说明）
    4. 端口（port）配置说明
    5. Webhook Secret（webhookSecret）配置说明
    6. 通信端点（commBridge.defaultEndpoint）配置说明
    7. 环境变量配置方法

    如果缺少任何部分，添加相应内容。确保配置说明准确、完整。
  </action>
  <verify>
    <automated>grep -A 40 "## 配置" README.md | grep -E "(port|webhookSecret|defaultEndpoint|GITHUB_WEBHOOK_SECRET)" | wc -l | grep -q "^4"</automated>
  </verify>
  <done>"配置"部分包含端口、secret、端点的完整配置说明</done>
</task>

<task type="auto">
  <name>Task 5: 验证并完善 GitHub Webhook 设置说明（DOC-03）</name>
  <files>README.md</files>
  <action>
    验证 README.md 中的"GitHub Webhook 设置"部分（约第 130-156 行）是否包含：
    1. 在 GitHub 仓库中添加 webhook 的步骤
    2. Payload URL 配置（包含端口 3461 和 /webhook 路径）
    3. Content type 设置（application/json）
    4. Secret 配置说明（与 config.json 中的 webhookSecret 匹配）
    5. 推荐的事件类型列表（issues、pull_request、issue_comment、release）
    6. 验证设置的命令（curl 健康检查）

    如果缺少任何部分，添加相应内容。

    添加具体示例：
    ```
    Payload URL 示例：
    - 本地测试：http://localhost:3461/webhook
    - 生产环境：https://your-domain.com:3461/webhook
    ```

    添加事件类型详细说明：
    - **Issues** — opened、closed、reopened
    - **Pull Requests** — opened、closed、merged、ready_for_review
    - **Issue Comments** — created
    - **Releases** — published
  </action>
  <verify>
    <automated>grep -A 25 "## GitHub Webhook 设置" README.md | grep -E "(Payload URL|Content type|Secret|Events|curl)" | wc -l | grep -q "^5"</automated>
  </verify>
  <done>"GitHub Webhook 设置"部分包含 URL、secret、事件类型的完整配置说明</done>
</task>

<task type="auto">
  <name>Task 6: 更新"开发路线图"部分</name>
  <files>README.md</files>
  <action>
    更新 README.md 中的"开发路线图"部分（约第 271-285 行），反映实际完成情况：

    ```
    **Phase 1:** ✓ HTTP 服务器基础
    **Phase 2:** ✓ 签名验证
    **Phase 3:** ✓ 事件路由和去重
    **Phase 4:** ✓ Issues 事件处理程序
    **Phase 5:** ✓ Pull Request 事件处理程序
    **Phase 6:** ✓ Comment 和 Release 事件处理程序
    **Phase 7:** ✓ 消息格式化模块
    **Phase 8:** ✓ C4 通信桥集成
    **Phase 9:** ✓ 配置管理
    **Phase 10:** ✓ 生命周期和 PM2 集成
    **Phase 11:** ✓ 组件元数据（SKILL.md）
    **Phase 12:** 🔄 文档和测试验证（当前阶段）
    ```

    移除或注释掉不准确的路线图描述（如"Phase 3-12"的未来规划）。
  </action>
  <verify>
    <automated>grep -A 15 "## 开发路线图" README.md | grep "✓" | wc -l | grep -q "^11"</automated>
  </verify>
  <done>"开发路线图"部分准确反映所有 11 个已完成阶段和当前 Phase 12</done>
</task>

<task type="auto">
  <name>Task 7: 添加"支持的 GitHub 事件"详细说明</name>
  <files>README.md</files>
  <action>
    在"功能特性"部分之后，添加新的"支持的 GitHub 事件"部分，详细说明支持的事件类型和动作：

    ```markdown
    ## 支持的 GitHub 事件

    ### Issues（问题）
    - **opened** — 新问题创建
    - **closed** — 问题关闭
    - **reopened** — 问题重新打开

    ### Pull Requests（拉取请求）
    - **opened** — 新 PR 创建
    - **closed** — PR 关闭（未合并）
    - **merged** — PR 合并
    - **ready_for_review** — Draft PR 准备好审查

    ### Issue Comments（问题评论）
    - **created** — 新评论发布

    ### Releases（发布）
    - **published** — 新发布发布
    ```

    添加每个事件的通知示例（可选）。
  </action>
  <verify>
    <automated>grep -A 20 "## 支持的 GitHub 事件" README.md | grep -E "(Issues|Pull Requests|Issue Comments|Releases)" | wc -l | grep -q "^4"</automated>
  </verify>
  <done>"支持的 GitHub 事件"部分详细列出所有支持的事件类型和动作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户 → README.md | 用户依据文档进行配置和操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | 配置示例 | accept | 文档仅提供示例，实际 secret 由用户生成 |
| T-12-02 | Tampering | 安装命令 | accept | 文档使用官方包来源（npm、GitHub） |
| T-12-03 | Information Disclosure | webhookSecret | mitigate | 文档明确警告切勿在日志中记录 secret，强调使用环境变量 |
</threat_model>

<verification>
## 整体验证检查

1. **文档完整性验证** — 运行以下命令验证所有必需部分存在：
   ```bash
   grep -E "(## 安装|## 配置|## GitHub Webhook 设置|## 当前状态|## 支持的 GitHub 事件)" README.md | wc -l
   ```
   预期输出：5（所有标题都存在）

2. **配置说明验证** — 验证配置选项表格包含关键字段：
   ```bash
   grep -A 20 "配置选项" README.md | grep -E "(port|webhookSecret|defaultEndpoint)"
   ```
   预期输出：至少 3 个匹配

3. **Webhook 设置验证** — 验证 webhook 设置说明完整：
   ```bash
   grep -A 25 "## GitHub Webhook 设置" README.md | grep -E "(Payload URL|Secret|Events)"
   ```
   预期输出：至少 3 个匹配

4. **状态更新验证** — 验证当前状态已更新为 Phase 11：
   ```bash
   grep "Phase 11" README.md
   ```
   预期输出：至少 1 个匹配

5. **README 可读性验证** — 手动检查 README.md 结构合理、无错别字、命令可执行
</verification>

<success_criteria>
## 成功标准

1. ✅ README.md 的"当前状态"更新为"Phase 11 已完成"
2. ✅ "功能特性"部分包含所有核心功能（事件处理、消息格式化、去重、异步通信、配置验证）
3. ✅ "安装"部分包含 CLI 安装、手动安装、系统要求和安装后验证
4. ✅ "配置"部分包含配置文件路径、完整示例、选项表格、端口/secret/端点说明、环境变量
5. ✅ "GitHub Webhook 设置"部分包含添加步骤、URL 配置、secret 配置、事件类型、验证方法
6. ✅ "支持的 GitHub 事件"部分详细列出 issues、pull_request、issue_comment、release 事件
7. ✅ "开发路线图"部分反映所有 11 个已完成阶段和当前 Phase 12
8. ✅ 所有文档内容准确、清晰、可执行
9. ✅ 无错别字或格式错误
</success_criteria>

<output>
完成后创建 `.planning/phases/12-documentation-and-testing/12-01-SUMMARY.md`
</output>
